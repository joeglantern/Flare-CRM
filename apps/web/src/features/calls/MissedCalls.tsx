import { CallsListScreen } from './CallsList';

/** Missed calls is the calls list in its second mode (Calls · Missed calls). */
export function MissedCallsScreen() {
  return <CallsListScreen missed />;
}
